export const raf: Raf;
declare class Raf {
    callbacks: any[];
    now: number;
    add(callback: any, priority?: number): () => void;
    remove(callback: any): void;
    raf: (now: any) => void;
}
export {};
